# 请求：任务列表溢出时显示纵向滚动条

- 请求标识：`bilicatch-task-scrollbar-visibility-20260916`
- 来源：用户于 2026-09-16 的直接缺陷报告
- 目标：任务数量超过软件可视高度时，主内容区显示稳定、可辨识的纵向滚动条。
- 范围：应用壳层唯一滚动容器 `.content-scroll` 的滚动条样式及回归保护。
- 禁止解释：不在任务列表内新增第二层滚动容器，不修改任务业务逻辑、IPC 或数据结构。

## 用户意图合同

- observed：任务超出软件高度后，没有出现纵向滚动条。
- expected：内容溢出时滚动条可见，且用户能滚动访问全部任务。
- success：保留 `.content-scroll` 的唯一滚动所有权，同时为 Chromium/WebView2 声明稳定滚动槽和显式轨道/滑块样式。
