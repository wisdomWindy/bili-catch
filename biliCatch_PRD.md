# BiliCatch — B站视频/音频下载器 产品需求文档（PRD）

| 项目         | 内容                                                         |
| ------------ | ------------------------------------------------------------ |
| **文档状态** | 正式发布                                                     |
| **产品名称** | BiliCatch                                                    |
| **产品定位** | 基于 Tauri 的跨平台桌面端 B 站音视频下载工具                 |
| **目标平台** | Windows / macOS / Linux                                      |
| **目标用户** | 需要离线保存 B 站视频与音频内容的学习者、创作者、媒体归档用户 |
| **编制日期** | 2026-09-10                                                   |


## 一、产品概述

### 1.1 产品背景

B 站（Bilibili）作为国内主流视频平台，采用 MPEG-DASH 自适应流媒体协议，将视频与音频分离为独立的媒体轨道进行分发。普通用户无法直接获取原始媒体流文件，存在离线学习、音频提取、内容归档等场景下的真实需求。当前市面上的同类工具多依赖 Electron 构建，存在安装包体积大、内存占用高等问题。Tauri 框架以 Rust 为后端、系统 WebView 为渲染层，可在保证功能完整性的前提下显著降低资源占用。

### 1.2 产品目标

- **核心目标**：提供稳定、高效的 B 站视频与音频下载能力，支持音频独立下载。
- **体验目标**：简洁直观的桌面端操作体验，原生系统集成（托盘、通知、文件对话框）。
- **技术目标**：主程序安装包控制在 15MB 以内（不含 FFmpeg），冷启动时间 < 1.5s，下载速度较同类工具提升 30% 以上。

### 1.3 产品范围

| 范围内                       | 范围外（V1.0 不做）              |
| ---------------------------- | -------------------------------- |
| 单个视频/音频下载            | 直播流录制                       |
| 合集/番剧批量下载            | 弹幕下载与渲染                   |
| 音频独立下载（MP3/M4A/FLAC） | 视频编辑/转码                    |
| 扫码登录与 Cookie 管理       | 字幕下载（V1.1 规划）            |
| 下载队列管理与断点续传       | 播放列表/收藏夹导入（V1.2 规划） |

### 1.4 登录必要性说明

应用采用“匿名可用，登录解锁高清/无损”的策略。匿名状态可解析视频、下载 ≤480P 视频和 ≤64K 音频；登录后可下载 1080P 及以上、4K/8K/HDR、HiRes 无损音频，并访问账号有权观看的受限内容。应用内通过扫码登录 B 站账号，Cookie 加密保存于本地，应用重启后自动恢复登录状态。


## 二、技术选型

### 2.1 整体技术架构

采用 Tauri v2 作为桌面应用框架，Rust 负责所有核心业务逻辑（API 请求、下载引擎、FFmpeg 调度、文件 I/O），前端框架负责 UI 渲染与用户交互。Tauri 官方推荐使用 Vite 作为 SPA 框架的构建工具，本项目选择 **Vue 3 + TypeScript + Vite** 作为前端技术栈，原因如下：

- BiliTools 等成熟的开源 B 站下载工具已验证 Vue 3 + Tauri v2 的技术可行性，具有良好的实践参考；
- Vue 3 的组合式 API 与 Tauri 的 IPC 调用模式天然契合，状态管理清晰；
- Element Plus 或 Naive UI 提供丰富的桌面端组件，减少 UI 开发工作量。

### 2.2 详细技术栈清单

| 层级              | 技术选型                                  | 版本要求   | 选型依据                                               |
| ----------------- | ----------------------------------------- | ---------- | ------------------------------------------------------ |
| **桌面框架**      | Tauri                                     | v2.x       | 体积小、原生集成度高，官方推荐 Vite 作为前端构建工具   |
| **前端框架**      | Vue 3 + TypeScript                        | 3.5+ / 5.x | 组合式 API，生态成熟，BiliTools 已验证                 |
| **构建工具**      | Vite                                      | 6.x        | Tauri 官方推荐，HMR 开发体验好                         |
| **UI 组件库**     | Naive UI                                  | 2.x        | 轻量、TypeScript 原生支持、暗色主题完善                |
| **状态管理**      | Pinia                                     | 2.x        | Vue 3 官方推荐                                         |
| **Rust 后端框架** | Tauri Runtime                             | v2.x       | 内置 IPC、事件系统、插件体系                           |
| **HTTP 客户端**   | reqwest                                   | 0.12+      | 异步、支持流式下载、Cookie 管理                        |
| **异步运行时**    | Tokio                                     | 1.x        | Rust 生态标准异步运行时                                |
| **下载引擎**      | 自研多线程下载器 + aria2（可选）          | —          | 参考 tauri-plugin-download 的状态驱动模型              |
| **媒体处理**      | FFmpeg（内置 sidecar）                    | 7.x        | 音视频合并、音频格式转换的标准工具，随安装程序一并安装 |
| **持久化存储**    | tauri-plugin-store                        | 2.x        | 官方插件，键值对持久化，跨应用重启保留                 |
| **系统集成**      | tauri-plugin-dialog / notification / tray | 2.x        | 原生文件对话框、系统通知、托盘图标                     |
| **自动更新**      | tauri-plugin-updater                      | 2.x        | 官方更新插件，支持静态 JSON 或更新服务器               |

### 2.3 Rust 核心依赖

| Crate                  | 用途                                   |
| ---------------------- | -------------------------------------- |
| `reqwest`              | HTTP 请求（B 站 API 调用、媒体流下载） |
| `tokio`                | 异步任务调度、并发控制                 |
| `serde` / `serde_json` | JSON 序列化与反序列化                  |
| `thiserror`            | 统一错误类型定义                       |
| `md5` / `sha2`         | WBI 签名计算                           |
| `tokio-util`           | 流式读写与进度追踪                     |
| `regex`                | URL 解析与视频 ID 提取                 |
| `url`                  | URL 规范化处理                         |

### 2.4 前端核心依赖

| 包                                | 用途                  |
| --------------------------------- | --------------------- |
| `@tauri-apps/api`                 | IPC 调用、事件监听    |
| `@tauri-apps/plugin-store`        | 设置持久化            |
| `@tauri-apps/plugin-dialog`       | 文件/目录选择对话框   |
| `@tauri-apps/plugin-notification` | 系统通知              |
| `@tauri-apps/plugin-updater`      | 应用自动更新          |
| `qrcode`                          | 登录二维码渲染        |
| `vue-router`                      | 页面路由（Hash 模式） |


## 三、系统架构设计

### 3.1 分层架构

```
┌─────────────────────────────────────────────────────────┐
│                    前端层（Vue 3 + TypeScript）            │
│  ┌──────────┐ ┌──────────┐ ┌──────────┐ ┌─────────────┐ │
│  │ 解析页    │ │ 下载页    │ │ 任务页    │ │ 设置页       │ │
│  └──────────┘ └──────────┘ └──────────┘ └─────────────┘ │
│              Pinia Store（全局状态）                       │
│              Tauri IPC / Event Listener                   │
└──────────────────────┬──────────────────────────────────┘
                       │ IPC (invoke / emit)
┌──────────────────────┴──────────────────────────────────┐
│                  Rust 后端层（Tauri Runtime）              │
│  ┌────────────────────────────────────────────────────┐  │
│  │              Command Layer（IPC 命令处理）            │  │
│  ├────────────────────────────────────────────────────┤  │
│  │              Service Layer                          │  │
│  │  ┌──────────┐ ┌──────────┐ ┌──────────┐           │  │
│  │  │ URL 解析  │ │ 媒体解析  │ │ 登录管理  │           │  │
│  │  │ Service   │ │ Service   │ │ Service   │           │  │
│  │  └──────────┘ └──────────┘ └──────────┘           │  │
│  │  ┌──────────┐ ┌──────────┐ ┌──────────┐           │  │
│  │  │ 下载引擎  │ │ 任务队列  │ │ 格式转换  │           │  │
│  │  │ Downloader│ │ Scheduler │ │ FFmpeg    │           │  │
│  │  └──────────┘ └──────────┘ └──────────┘           │  │
│  ├────────────────────────────────────────────────────┤  │
│  │              Infrastructure Layer                   │  │
│  │  ┌──────────┐ ┌──────────┐ ┌──────────┐           │  │
│  │  │ WBI 签名  │ │ Cookie   │ │ 持久化    │           │  │
│  │  │ Signer    │ │ Store    │ │ Store     │           │  │
│  │  └──────────┘ └──────────┘ └──────────┘           │  │
│  └────────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────┘
```

### 3.2 数据流

1. **解析流程**：用户输入 URL → 前端调用 `invoke('parse_video')` → Rust 解析 URL 提取视频 ID → 调用 B 站 API（携带 WBI 签名）→ 返回视频元数据 + 可用流列表 → 前端展示。
2. **下载流程**：用户确认下载参数 → 前端调用 `invoke('create_download_task')` → Rust 创建任务并加入队列 → 下载引擎开始下载 → 通过 `emit` 向前端推送进度事件 → 前端实时更新 UI。
3. **登录流程**：用户请求扫码登录 → Rust 调用 B 站二维码生成 API → 前端渲染二维码 → Rust 轮询扫码状态 → 登录成功后保存 Cookie → 通知前端。


## 四、功能模块与业务流程

### 4.0 模块与页面映射

应用采用单窗口 + 侧边导航布局，主内容区域通过 Vue Router 切换。路由使用 Hash 模式。页面不单独成章，而是归属到对应功能模块中说明。

| 模块                         | 页面/入口                          | 路由        | 说明                             |
| ---------------------------- | ---------------------------------- | ----------- | -------------------------------- |
| 模块一：视频/音频解析        | 下载中心页                         | `/download` | 默认页，URL 输入、解析结果展示   |
| 模块二：音频独立下载         | 下载中心页内“仅音频”模式           | `/download` | 复用下载中心页，条件显示音频选项 |
| 模块三：视频下载与音视频合并 | 下载中心页内“视频+音频/仅视频”模式 | `/download` | 复用下载中心页，条件显示视频选项 |
| 模块四：下载任务管理         | 任务列表页                         | `/tasks`    | 任务队列、进度、暂停/恢复/取消   |
| 模块五：登录与认证           | 登录页                             | `/login`    | 扫码登录、登录状态管理           |
| 模块六：设置管理             | 设置页                             | `/settings` | 全局配置持久化                   |
| 模块七：系统集成             | 托盘菜单、系统通知、文件对话框     | 无独立页面  | 系统级能力，不占独立路由         |

全局顶部栏包含：返回按钮、应用名、登录状态入口、设置入口。侧边导航包含：下载中心、任务列表、设置。


### 模块一：视频/音频解析

**功能描述**：接收用户输入的 B 站链接或 BV/AV 号，解析视频元数据并展示可下载的媒体流列表。

**支持的输入格式**：

| 输入类型     | 示例                                          | 说明                                       |
| ------------ | --------------------------------------------- | ------------------------------------------ |
| 标准视频 URL | `https://www.bilibili.com/video/BV1xx411c7BF` | 完整链接                                   |
| 移动端 URL   | `https://m.bilibili.com/video/av170001`       | 自动规范化                                 |
| 短链接       | `https://b23.tv/xxxxxx`                       | 自动解析重定向                             |
| 纯 ID        | `BV1xx411c7BF` / `av170001`                   | 自动补全为完整 URL                         |
| 多 P 视频    | `...?p=3`                                     | 解析指定分 P，也支持仅输入主链接后选择分 P |

**页面结构：下载中心页 `/download`**

```
┌─────────────────────────────────────────────────────┐
│  ┌───────────────────────────────────────────────┐  │
│  │  🔗 粘贴B站视频链接或BV/AV号...     [解析]    │  │
│  └───────────────────────────────────────────────┘  │
│                                                       │
│  ┌─ 解析结果 ──────────────────────────────────────┐ │
│  │  [缩略图]  视频标题                              │ │
│  │            UP主: xxx  |  时长: 12:34            │ │
│  │                                                  │ │
│  │  ┌─ 分P选择（多P视频时显示）──────────────────┐ │ │
│  │  │  ☑ P1: 第一集    ☐ P2: 第二集    ☐ P3...  │ │ │
│  │  └──────────────────────────────────────────────┘ │ │
│  │                                                  │ │
│  │  下载类型:  ● 视频+音频   ○ 仅视频   ○ 仅音频    │ │
│  │                                                  │ │
│  │  清晰度:   [1080P ▼]    编码: [AVC ▼]           │ │
│  │  音频格式: [MP3 ▼]      码率: [320K ▼]          │ │
│  │  (仅音频模式下显示音频格式选项)                    │ │
│  │                                                  │ │
│  │  保存到:   [~/Downloads/BiliCatch]  [更改]      │ │
│  │                                                  │ │
│  │  [  添加到下载队列  ]    [  清空  ]              │ │
│  └──────────────────────────────────────────────────┘ │
└─────────────────────────────────────────────────────┘
```

**页面操作流程**：

| 操作             | 触发条件               | 行为                                                         |
| ---------------- | ---------------------- | ------------------------------------------------------------ |
| **解析按钮**     | URL 输入框非空         | 调用 `parse_video` → 展示加载态 → 渲染结果。输入为空时按钮禁用。 |
| **粘贴并解析**   | 剪贴板包含有效 URL     | 自动填充输入框并触发解析。                                   |
| **分P多选**      | 解析结果为多P视频      | 显示分P列表，支持全选/反选。默认选中第一集。                 |
| **下载类型切换** | 用户切换单选按钮       | 切换到“仅音频”时，隐藏清晰度/编码选项，显示音频格式/码率；切换到“视频+音频/仅视频”时，显示清晰度/编码选项。 |
| **添加到队列**   | 已解析且已选择分P      | 根据下载类型创建任务 → 提示“已添加到下载队列” → 自动跳转到 `/tasks`。 |
| **清空**         | 已有解析结果或输入内容 | 清空输入框、解析结果和已选选项，恢复初始状态。               |
| **拖拽输入**     | 拖拽链接到输入框区域   | 自动填充并触发解析。                                         |

**页面跳转逻辑**：

- 解析成功后，点击“添加到下载队列” → 跳转至 `/tasks`。
- 点击顶部“登录状态” → 跳转至 `/login`。
- 点击设置图标或侧边“设置” → 跳转至 `/settings`。
- 点击侧边“任务列表” → 跳转至 `/tasks`。

**解析业务流程**：

```
[用户粘贴 URL] → [前端校验格式] → [调用 parse_video 命令]
    → [Rust 解析 URL 提取视频 ID]
    → [检查登录状态 → 决定是否携带 Cookie]
    → [调用 WBI 签名 → 请求 playurl API]
    → [解析 JSON 响应 → 提取视频/音频流列表]
    → [返回前端渲染]
    → 异常分支：
        ├─ [链接无效] → 提示"请输入有效的B站链接"
        ├─ [视频不存在/已删除] → 提示"视频不存在或已被删除"
        ├─ [地区限制] → 提示"该视频在当前地区不可用"
        ├─ [需要登录] → 提示并引导至登录页
        └─ [WBI 签名失败] → 自动刷新密钥重试一次，仍失败则提示
```

**业务规则**：
- WBI 签名密钥缓存有效期约 12 小时，过期后自动刷新。
- 匿名请求的清晰度上限为 480P；登录后可获取 1080P 及以上清晰度。
- 多 P 视频解析后需展示分 P 列表，用户可选择指定分 P 或全部下载。
- 解析结果需缓存至 Pinia Store，避免重复请求。


### 模块二：音频独立下载

**功能描述**：从 B 站视频中提取音频流，支持转换为多种音频格式，独立于视频下载。

**支持的输出格式**：

| 格式      | 码率范围           | 适用场景                   |
| --------- | ------------------ | -------------------------- |
| MP3       | 128K / 192K / 320K | 通用兼容，便于移动设备播放 |
| M4A (AAC) | 原始码率           | 保留原始音质，文件体积小   |
| FLAC      | 无损               | 音乐收藏，需登录且视频支持 |

**页面结构：下载中心页“仅音频”模式**

当用户在下载中心页将“下载类型”切换为“仅音频”时：

- 隐藏“清晰度”“编码”选项。
- 显示“音频格式”下拉框（MP3 / M4A / FLAC）。
- 显示“码率”下拉框，仅当音频格式为 MP3 时可选 128K / 192K / 320K。
- “添加到下载队列”按钮文案保持，但创建的是音频下载任务。

**页面操作流程**：

1. 用户在解析结果区选择“仅音频”。
2. 选择输出格式（MP3 / M4A / FLAC）。
3. 若选择 MP3，选择码率（128K / 192K / 320K）；M4A 和 FLAC 使用原始码率，码率下拉禁用。
4. 确认下载目录。
5. 点击“添加到下载队列”。
6. 前端调用 `create_download_task`，任务类型为 `audio_only`。
7. 跳转至 `/tasks` 查看进度。

**业务流程**：

```
[用户在解析结果页选择"仅下载音频"]
    → [选择输出格式（MP3/M4A/FLAC）]
    → [选择码率（MP3 可选 128/192/320K）]
    → [确认下载目录]
    → [创建下载任务 → 仅下载音频流轨道]
    → [下载完成后 → FFmpeg 转换为目标格式]
    → [FFmpeg 执行: ffmpeg -i input.m4s -vn -acodec libmp3lame -b:a 320k output.mp3]
    → [清理临时文件 → 通知完成]
```

**业务规则**：
- 音频码率选择仅在 MP3 格式时生效；M4A 和 FLAC 使用原始码率。
- FLAC 格式需要视频源包含无损音频流，否则自动降级为 M4A 并提示用户。
- 文件命名规则：`{视频标题}.{扩展名}`，合集视频为 `{序号}_{标题}.{扩展名}`。
- 音频下载模式不下载视频轨道，节省带宽与存储空间。


### 模块三：视频下载与音视频合并

**功能描述**：下载完整的视频（含音频），通过 FFmpeg 进行无损封装合并。

**页面结构：下载中心页“视频+音频/仅视频”模式**

当用户在下载中心页选择“视频+音频”或“仅视频”时：

- 显示“清晰度”下拉框。
- 显示“编码”下拉框（AVC / HEVC / AV1）。
- 显示“保存到”路径选择器。
- 若选择“仅视频”，不下载音频轨道；若选择“视频+音频”，下载视频轨道和音频轨道并合并。

**页面操作流程**：

1. 用户选择“视频+音频”或“仅视频”。
2. 选择清晰度。未登录时仅展示 ≤480P；已登录时展示全部可用清晰度，并标注需登录的选项。
3. 选择编码格式。
4. 确认保存目录。
5. 点击“添加到下载队列”。
6. 前端调用 `create_download_task`，任务类型为 `video_audio` 或 `video_only`。
7. 跳转至 `/tasks` 查看进度。

**业务流程**：

```
[用户选择视频清晰度 + 编码格式]
    → [创建下载任务（视频轨道 + 音频轨道并行下载）]
    → [下载引擎：两个独立线程池并行拉取]
    → [下载完成 → 校验文件完整性]
    → [FFmpeg 无损封装:
        ffmpeg -i video.m4s -i audio.m4s -c copy -map 0:v:0 -map 1:a:0 output.mp4]
    → [删除临时 m4s 文件 → 通知完成]
```

FFmpeg 采用 `-c copy` 模式进行 Remuxing（重新封装），不涉及重新编码，保持 100% 原始画质。

**业务规则**：
- 视频与音频轨道必须并行下载，不能串行（否则下载时间翻倍）。
- 支持的编码格式：AVC (H.264)、HEVC (H.265)、AV1。
- 支持的清晰度：360P / 480P / 720P / 1080P / 1080P+ / 4K / 8K / HDR。
- 1080P 及以上清晰度需要登录账号。
- 下载过程中任一轨道失败，整个任务标记为失败，需支持重试。


### 模块四：下载任务管理

**功能描述**：对下载任务进行队列管理、进度监控、暂停/恢复/取消操作。

**页面结构：任务列表页 `/tasks`**

```
┌─────────────────────────────────────────────────────┐
│  [全部] [下载中] [已完成] [失败]      [暂停全部] [清空已完成] │
│                                                       │
│  ┌─────────────────────────────────────────────────┐ │
│  │ 📄 视频标题                       [下载中]       │ │
│  │    1080P · MP4 · 234MB                         │ │
│  │    ████████████░░░░░░  65%  2.3MB/s  ETA 1:20  │ │
│  │    [⏸ 暂停] [✕ 取消]                           │ │
│  ├─────────────────────────────────────────────────┤ │
│  │ 📄 另一个视频标题                  [已完成]       │ │
│  │    仅音频 · MP3 320K · 12MB                     │ │
│  │    ████████████████████  100%                   │ │
│  │    [📂 打开文件] [📁 打开目录]                  │ │
│  ├─────────────────────────────────────────────────┤ │
│  │ 📄 失败的视频                     [错误]         │ │
│  │    1080P · MP4                                  │ │
│  │    错误: 网络连接超时                            │ │
│  │    [🔄 重试] [🗑 删除]                         │ │
│  └─────────────────────────────────────────────────┘ │
└─────────────────────────────────────────────────────┘
```

**页面操作按钮与逻辑**：

| 按钮           | 可见状态                 | 操作逻辑                                 |
| -------------- | ------------------------ | ---------------------------------------- |
| **暂停**       | 下载中                   | 暂停下载，保存断点信息，状态变为“已暂停” |
| **恢复**       | 已暂停                   | 从断点继续下载，状态变为“下载中”         |
| **取消**       | 下载中 / 已暂停 / 等待中 | 终止下载，删除临时文件，状态变为“已取消” |
| **重试**       | 错误                     | 重置任务状态，从断点或从头开始重新下载   |
| **删除**       | 已完成 / 已取消 / 错误   | 从任务列表中移除记录（不删除已下载文件） |
| **打开文件**   | 已完成                   | 调用系统默认应用打开文件                 |
| **打开目录**   | 已完成                   | 调用文件管理器打开文件所在目录           |
| **清空已完成** | 列表中有已完成任务       | 批量移除所有已完成任务记录               |
| **暂停全部**   | 存在下载中任务           | 批量暂停所有下载中任务                   |

**进度更新机制**：Rust 后端通过 `app_handle.emit("download://progress", payload)` 向前端推送进度事件，前端使用 `listen` 监听并更新对应任务卡片。事件负载包含：任务 ID、进度百分比、下载速度、剩余时间、已下载字节数。

**业务流程**：

```
[创建任务] → [任务进入等待队列] → [调度器分配并发槽位]
    → [状态: 下载中] → [emit('download://progress', {id, progress, speed, eta})]
    → [前端实时更新进度条]
    → 分支：
        ├─ [用户暂停] → [暂停下载 → 保存断点信息]
        ├─ [用户取消] → [终止下载 → 清理临时文件 → 任务标记为已取消]
        ├─ [网络中断] → [自动重试（最多3次）→ 失败则标记为错误]
        └─ [下载完成] → [触发后处理（合并/转换）→ 标记为已完成]
```

**任务状态机**：

```
等待中(Pending) → 下载中(Downloading) → 处理中(Processing) → 已完成(Completed)
                     ↕ 暂停(Paused)                              ↓
                  错误(Error) ← 重试 ← ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─┘
                     ↓
                  已取消(Cancelled)
```

**业务规则**：
- 最大并发下载任务数默认为 3，用户可在设置中调整（范围 1-10）。
- 每个任务内部的多线程下载连接数默认为 8，可配置范围 1-32。
- 断点续传：利用 HTTP Range 头部实现，暂停/应用重启后可恢复。
- 自动重试：网络错误时自动重试，间隔采用指数退避（1s → 2s → 4s）。
- 任务完成后发送系统通知。

**页面跳转逻辑**：

- 侧边“下载中心” → `/download`。
- 侧边“设置” → `/settings`。
- 顶部登录状态 → `/login`。
- 任务完成通知点击后，若应用未聚焦则唤起主窗口并停留在 `/tasks`。


### 模块五：登录与认证

**功能描述**：支持 B 站账号登录以获取高清资源访问权限，采用扫码登录方式。

**页面结构：登录页 `/login`**

```
┌─────────────────────────────────────┐
│           扫码登录 B站账号            │
│                                     │
│         ┌───────────────┐           │
│         │               │           │
│         │   [二维码]     │           │
│         │               │           │
│         └───────────────┘           │
│                                     │
│      使用 B站 App 扫描二维码          │
│                                     │
│      [刷新二维码]                    │
│                                     │
│      登录后可下载 1080P 及以上清晰度   │
└─────────────────────────────────────┘
```

**页面操作流程**：

- 页面加载时自动请求二维码 → 渲染 → 开始轮询。
- 轮询状态实时更新提示文案：未扫码 / 已扫码待确认 / 已确认 / 已过期。
- 用户可点击“刷新二维码”重新生成。
- 登录成功后，自动关闭登录页或返回上一页，顶部登录状态更新。
- 已登录状态下访问该页，展示当前账号信息 + “退出登录”按钮。
- 点击“退出登录” → 清除本地 Cookie → 返回匿名状态。

**业务流程**：

```
[用户点击"登录"] → [调用 B 站二维码生成 API]
    → [获取 qrcode_key + 登录 URL]
    → [前端使用 qrcode 库渲染二维码图片]
    → [Rust 后端每 2s 轮询扫码状态]
    → 状态分支：
        ├─ [未扫码] → 继续轮询
        ├─ [已扫码待确认] → 前端提示"已扫码，请在手机上确认"
        ├─ [已确认] → [获取 Cookie → 加密存储 → 刷新登录状态]
        ├─ [已过期] → [前端提示"二维码已过期" → 提供刷新按钮]
        └─ [已取消] → [前端提示"登录已取消"]
```

**业务规则**：
- 二维码有效期约 180 秒，过期后自动停止轮询。
- 登录成功后，Cookie 通过 tauri-plugin-store 持久化保存，应用重启后自动恢复登录状态。
- 支持手动退出登录，退出后清除本地 Cookie。
- 登录状态检测：每次解析请求前检查 Cookie 有效性，过期则提示重新登录。
- 登录非强制；匿名状态仍可使用基础下载能力。


### 模块六：设置管理

**功能描述**：管理应用的全局配置项，所有设置通过 tauri-plugin-store 持久化。

**页面结构：设置页 `/settings`**

设置页采用分组式设置表单，分为“下载设置”“外观设置”“系统设置”“关于”四个分组。

| 分组     | 设置项         | 控件类型                       |
| -------- | -------------- | ------------------------------ |
| 下载设置 | 下载目录       | 路径选择器 + 输入框            |
| 下载设置 | 临时文件目录   | 路径选择器 + 输入框            |
| 下载设置 | 最大并发任务数 | 滑块 + 数值显示                |
| 下载设置 | 单任务连接数   | 滑块 + 数值显示                |
| 下载设置 | 默认视频清晰度 | 下拉选择                       |
| 下载设置 | 默认音频格式   | 下拉选择                       |
| 外观设置 | 主题           | 下拉选择（亮色/暗色/跟随系统） |
| 外观设置 | 语言           | 下拉选择（简体中文/English）   |
| 系统设置 | 下载完成后通知 | 开关                           |
| 系统设置 | 关闭窗口行为   | 下拉选择                       |
| 系统设置 | 自动检查更新   | 开关                           |
| 关于     | 版本号         | 文本展示                       |
| 关于     | 检查更新       | 按钮                           |
| 关于     | 开源许可       | 链接                           |

**页面操作流程**：

- 所有设置修改后即时保存至 tauri-plugin-store，无需手动保存按钮。
- 点击“下载目录”或“临时文件目录”后的选择按钮 → 调用 `tauri-plugin-dialog` 打开系统目录选择框 → 选择后回填并保存。
- 滑块拖动时实时显示数值，释放后保存。
- 点击“检查更新” → 调用更新插件 → 有更新则提示，无更新则提示“已是最新版本”。
- 点击“开源许可” → 打开系统浏览器访问许可页面。
- 主题切换即时生效；语言切换即时生效，无需重启。

**设置项默认值**：

| 设置项         | 类型        | 默认值                  | 说明                    |
| -------------- | ----------- | ----------------------- | ----------------------- |
| 下载目录       | 路径选择    | `~/Downloads/BiliCatch` | 媒体文件保存路径        |
| 临时文件目录   | 路径选择    | 系统临时目录            | m4s 临时文件存放        |
| 最大并发任务数 | 数值 (1-10) | 3                       | 同时下载的任务数量      |
| 单任务连接数   | 数值 (1-32) | 8                       | 每个任务的并发连接数    |
| 默认视频清晰度 | 下拉选择    | 1080P                   | 解析后默认选中的清晰度  |
| 默认音频格式   | 下拉选择    | MP3                     | 音频下载的默认格式      |
| 主题           | 下拉选择    | 跟随系统                | 亮色 / 暗色 / 跟随系统  |
| 下载完成后通知 | 开关        | 开启                    | 是否发送系统通知        |
| 关闭窗口行为   | 下拉选择    | 最小化到托盘            | 最小化到托盘 / 直接退出 |
| 自动检查更新   | 开关        | 开启                    | 启动时检查新版本        |

**页面跳转逻辑**：

- 侧边“下载中心” → `/download`。
- 侧边“任务列表” → `/tasks`。
- 顶部登录状态 → `/login`。


### 模块七：系统集成

**功能描述**：利用 Tauri 原生能力实现桌面端系统级功能，无独立页面，主要通过托盘菜单、系统通知、文件对话框和窗口行为体现。

| 功能           | 实现方式                    | 说明                                            |
| -------------- | --------------------------- | ----------------------------------------------- |
| **系统托盘**   | `TrayIconBuilder`           | 托盘图标 + 右键菜单（显示主窗口/暂停所有/退出） |
| **系统通知**   | `tauri-plugin-notification` | 下载完成、下载失败、更新可用时发送通知          |
| **文件对话框** | `tauri-plugin-dialog`       | 选择下载目录、打开文件所在位置                  |
| **窗口管理**   | Tauri Window API            | 记忆窗口位置/大小，关闭行为可配置               |
| **自动更新**   | `tauri-plugin-updater`      | 启动时检查更新，静默下载 + 提示安装             |
| **拖拽输入**   | Tauri DragDrop              | 支持将链接拖拽到窗口自动解析                    |

**托盘菜单操作逻辑**：

| 菜单项     | 操作逻辑                                 |
| ---------- | ---------------------------------------- |
| 显示主窗口 | 唤起并聚焦主窗口                         |
| 暂停所有   | 暂停所有下载中任务                       |
| 退出       | 退出应用；若存在下载中任务，弹出确认提示 |

**系统通知触发场景**：

- 单个任务下载完成 → 通知“下载完成：{文件名}”。
- 单个任务下载失败 → 通知“下载失败：{文件名}，可重试”。
- 检查到新版本 → 通知“发现新版本，点击更新”。


## 五、业务规则与约束

### 5.1 功能约束

| 约束项           | 规则                                                   |
| ---------------- | ------------------------------------------------------ |
| 匿名下载清晰度   | ≤ 480P                                                 |
| 登录后下载清晰度 | 最高 8K + HDR（取决于账号权限和视频源）                |
| 音频码率         | 匿名：≤ 64K；登录：最高 192K + HiRes 无损              |
| 单次解析分P数    | 无限制，但批量下载时任务队列上限为 100 个              |
| 任务队列上限     | 100 个任务，超出时禁止添加并提示                       |
| 文件名长度       | 自动截断至 200 字符，移除非法字符 `\ / : * ? " < > \|` |
| 临时文件         | 下载完成后自动删除；取消任务时立即删除                 |

### 5.2 性能约束

| 指标             | 目标值                                             |
| ---------------- | -------------------------------------------------- |
| 冷启动时间       | < 1.5s                                             |
| 解析响应时间     | < 3s（正常网络条件下）                             |
| 下载速度         | 充分利用带宽，多线程下载速度 ≥ 单线程的 3 倍       |
| 内存占用（空闲） | < 80MB                                             |
| 安装包体积       | 主程序 < 15MB；含 FFmpeg 内置 sidecar 后约 45–65MB |
| FFmpeg 内置体积  | 约 30-50MB（已包含在安装包内，随应用一并安装）     |

### 5.3 兼容性约束

| 平台    | 最低版本                   | 架构        |
| ------- | -------------------------- | ----------- |
| Windows | Windows 10 (1809+)         | x64         |
| macOS   | macOS 10.15+               | x64 / ARM64 |
| Linux   | Ubuntu 20.04+ / Fedora 36+ | x64         |

### 5.4 安全与合规

- 用户 Cookie 以加密形式存储于本地，不上传至任何服务器。
- 应用启动时检查更新，更新包需通过签名验证。
- 不绕过 B 站付费内容保护机制，仅下载用户有权访问的内容。
- 应用内需展示免责声明：工具仅用于个人学习与合法用途，用户需自行承担内容版权责任。

### 5.5 错误处理规范

采用 `thiserror` + Tagged Enum 模式统一错误类型，所有 IPC 命令返回 `Result<T, AppError>`，前端根据错误码展示对应的用户提示。

| 错误码 | 场景            | 用户提示                    |
| ------ | --------------- | --------------------------- |
| `E001` | URL 格式无效    | 请输入有效的B站链接         |
| `E002` | 视频不存在      | 视频不存在或已被删除        |
| `E003` | 需要登录        | 请先登录以获取该清晰度      |
| `E004` | 地区限制        | 该视频在当前地区不可用      |
| `E005` | WBI 签名失败    | 请求签名失败，请稍后重试    |
| `E006` | 网络超时        | 网络连接超时，请检查网络    |
| `E007` | 磁盘空间不足    | 磁盘空间不足，请清理后重试  |
| `E008` | FFmpeg 执行失败 | 媒体处理失败，请检查 FFmpeg |
| `E009` | 下载中断        | 下载中断，已自动重试        |
| `E010` | 二维码过期      | 二维码已过期，请刷新        |


## 六、非功能性需求

### 6.1 可维护性

- Rust 后端代码按 Service/Infrastructure 分层，模块间通过 Trait 解耦。
- 前端组件按页面维度组织，全局状态统一由 Pinia 管理。
- 关键路径添加日志记录，日志文件按日期轮转，保留最近 7 天。

### 6.2 可扩展性

- 解析服务预留多平台接口（未来可扩展 YouTube、Twitter 等）。
- 下载引擎抽象为 `Downloader` trait，支持替换底层实现。
- 前端路由和侧边导航支持动态注册，便于后续增加新功能页。

### 6.3 国际化

- 前端使用 `vue-i18n` 实现多语言支持。
- V1.0 内置简体中文，预留英文语言包结构。
- 设置页语言切换即时生效，无需重启应用。

### 6.4 自动更新

- 采用 `tauri-plugin-updater`，通过 GitHub Releases 分发更新。
- 启动时静默检查更新，发现新版本后提示用户“立即更新”或“稍后”。
- 更新包通过 Ed25519 签名验证，确保完整性。
- 更新包中已包含 FFmpeg 内置 sidecar，无需单独更新 FFmpeg。


## 七、打包与分发产物

### 7.1 打包方式

使用 Tauri v2 内置打包能力，执行：

```bash
tauri build
```

按目标平台生成对应安装包。交叉编译或指定目标时：

```bash
tauri build --target x86_64-pc-windows-msvc
tauri build --target x86_64-apple-darwin
tauri build --target aarch64-apple-darwin
tauri build --target x86_64-unknown-linux-gnu
```

`tauri.conf.json` 中关键配置：

```json
{
  "bundle": {
    "active": true,
    "targets": ["nsis", "msi", "dmg", "app", "deb", "rpm", "appimage"],
    "icon": ["icons/32x32.png", "icons/128x128.png", "icons/icon.icns", "icons/icon.ico"],
    "externalBin": ["binaries/ffmpeg"],
    "resources": [],
    "updater": {
      "active": true,
      "endpoints": ["https://github.com/xxx/BiliCatch/releases/latest/download/latest.json"],
      "pubkey": "Ed25519 公钥"
    }
  }
}
```

### 7.2 各平台产物清单

所有平台安装包均已内置 FFmpeg，无需单独分发 FFmpeg 可执行文件。

| 平台    | 架构  | 安装包产物                                                   | 自动更新产物                                                 | 说明                                                 |
| ------- | ----- | ------------------------------------------------------------ | ------------------------------------------------------------ | ---------------------------------------------------- |
| Windows | x64   | `BiliCatch_x.x.x_x64-setup.exe`（NSIS）<br>`BiliCatch_x.x.x_x64_en-US.msi` | `BiliCatch_x.x.x_x64-setup.nsis.zip` + `.sig`<br>`BiliCatch_x.x.x_x64_en-US.msi.zip` + `.sig` | 推荐 NSIS 安装包；MSI 用于企业分发；均内置 FFmpeg    |
| Windows | x64   | `BiliCatch_x.x.x_x64-portable.zip`（可选）                   | —                                                            | 便携版，解压即用，不写注册表；已内置 FFmpeg          |
| macOS   | x64   | `BiliCatch_x.x.x_x64.dmg`<br>`BiliCatch.app`                 | `BiliCatch_x.x.x_x64.app.tar.gz` + `.sig`                    | 必须签名 + 公证；已内置 FFmpeg                       |
| macOS   | ARM64 | `BiliCatch_x.x.x_aarch64.dmg`<br>`BiliCatch.app`             | `BiliCatch_x.x.x_aarch64.app.tar.gz` + `.sig`                | Apple Silicon 原生；已内置 FFmpeg                    |
| Linux   | x64   | `bilicatch_x.x.x_amd64.deb`<br>`bilicatch-x.x.x-1.x86_64.rpm`<br>`BiliCatch_x.x.x_amd64.AppImage` | `BiliCatch_x.x.x_amd64.AppImage.tar.gz` + `.sig`             | AppImage 免安装；deb/rpm 走包管理器；均已内置 FFmpeg |
| 通用    | —     | `latest.json`                                                | —                                                            | 自动更新清单，包含各平台版本、下载 URL、签名         |

### 7.3 FFmpeg 内置打包

FFmpeg 作为应用内置组件，在打包时通过 Tauri 的 `externalBin` 机制嵌入安装程序，随应用一并安装到用户设备。用户安装 BiliCatch 后即可直接使用 FFmpeg 进行音视频合并与格式转换，**无需单独下载或配置 FFmpeg，也不单独分发 FFmpeg 文件**。

**目录结构示例（仅用于构建时组织二进制文件）：**

```
src-tauri/
  binaries/
    ffmpeg-x86_64-pc-windows-msvc.exe
    ffmpeg-x86_64-apple-darwin
    ffmpeg-aarch64-apple-darwin
    ffmpeg-x86_64-unknown-linux-gnu
```

**Tauri 配置：**

```json
{
  "bundle": {
    "externalBin": ["binaries/ffmpeg"]
  }
}
```

打包时 Tauri 会根据目标平台自动选择对应文件，重命名后放入应用资源目录（Windows 安装目录、macOS `.app/Contents/Resources`、Linux 应用目录等）。运行时通过 `tauri::process::Command` 或 `tauri-plugin-shell` 的 sidecar API 调用，路径由 Tauri 自动解析。

**关键点：**

- FFmpeg 已内嵌于各平台安装包中，安装后即可用。
- 自动更新时，FFmpeg 随主程序一起更新，无需额外操作。
- 用户无需关心 FFmpeg 的存在，应用内部自动调用。

### 7.4 自动更新产物

每次发布新版本时，除安装包外还需上传：

- `latest.json`：更新清单，格式示例：

```json
{
  "version": "1.0.1",
  "notes": "修复下载中断问题",
  "pub_date": "2026-09-10T12:00:00Z",
  "platforms": {
    "windows-x86_64": {
      "signature": "内容签名",
      "url": "https://github.com/xxx/BiliCatch/releases/download/v1.0.1/BiliCatch_1.0.1_x64-setup.nsis.zip"
    },
    "darwin-x86_64": {
      "signature": "内容签名",
      "url": "https://github.com/xxx/BiliCatch/releases/download/v1.0.1/BiliCatch_1.0.1_x64.app.tar.gz"
    },
    "darwin-aarch64": {
      "signature": "内容签名",
      "url": "https://github.com/xxx/BiliCatch/releases/download/v1.0.1/BiliCatch_1.0.1_aarch64.app.tar.gz"
    },
    "linux-x86_64": {
      "signature": "内容签名",
      "url": "https://github.com/xxx/BiliCatch/releases/download/v1.0.1/BiliCatch_1.0.1_amd64.AppImage.tar.gz"
    }
  }
}
```

- 每个更新包对应的 `.sig` 签名文件。
- 所有产物上传至 GitHub Releases，由 `latest.json` 指向。
- 更新包中已包含 FFmpeg，更新后 FFmpeg 同步更新，无需单独处理。

### 7.5 代码签名与公证

| 平台    | 要求   | 说明                                                         |
| ------- | ------ | ------------------------------------------------------------ |
| Windows | 可选   | 使用 EV 代码签名证书，减少 SmartScreen 警告；不签名也可运行，但会有提示 |
| macOS   | 必须   | Apple Developer ID 签名 + 公证（notarization），否则无法直接打开 |
| Linux   | 无强制 | 无需签名，AppImage 可直接运行                                |

### 7.6 产物命名规范

统一格式：

```
BiliCatch_{version}_{platform}_{arch}.{ext}
```

示例：

- `BiliCatch_1.0.0_x64-setup.exe`
- `BiliCatch_1.0.0_x64.dmg`
- `BiliCatch_1.0.0_aarch64.dmg`
- `BiliCatch_1.0.0_amd64.AppImage`
- `BiliCatch_1.0.0_amd64.deb`

### 7.7 打包检查清单

发布前确认：

- [ ] 主程序版本号已更新（`tauri.conf.json` 与 `Cargo.toml`、`package.json` 一致）
- [ ] FFmpeg 各平台二进制已放入 `src-tauri/binaries/`，并已通过 `externalBin` 配置内嵌到安装包
- [ ] 图标资源完整（`.ico`、`.icns`、PNG）
- [ ] Windows 安装包可正常安装/卸载，安装后 FFmpeg 可被应用调用
- [ ] macOS 已签名并公证，可正常打开，安装后 FFmpeg 可被应用调用
- [ ] Linux deb/rpm/AppImage 可正常安装或运行，安装后 FFmpeg 可被应用调用
- [ ] `latest.json` 与 `.sig` 已生成并上传
- [ ] 自动更新从旧版本可成功升级，更新后 FFmpeg 仍可用
- [ ] 安装包体积符合预期（主程序 <15MB，含 FFmpeg 约 45–65MB）


## 八、风险与应对

| 风险项             | 影响                   | 应对措施                                                     |
| ------------------ | ---------------------- | ------------------------------------------------------------ |
| B站 API 变更       | 解析功能失效           | 将 API 调用封装为独立模块，便于快速适配；关注 bilibili-API-collect 社区更新 |
| WBI 签名算法更新   | 所有 API 请求失败      | 密钥获取逻辑独立，算法变更时只需更新签名模块                 |
| FFmpeg 兼容性      | 合并/转换失败          | 使用静态编译的 FFmpeg 二进制，按平台分别测试；已内嵌安装包，减少环境差异 |
| 反爬/风控          | 请求被拒绝（HTTP 412） | 实现合理的请求间隔，使用真实 User-Agent 和 Referer，支持 Cookie 携带 |
| 下载速度受限于 CDN | 用户体验下降           | 实现多线程分片下载，支持 DASH 分片并行拉取                   |

