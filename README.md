# BiliCatch

BiliCatch 是一个基于 Tauri 2、Vue 3 与 TypeScript 的桌面视频/音频下载器。

## 开发命令

```sh
npm install
npm run dev
npm test
npm run build
npm run tauri dev
```

需求、规格、计划与验收记录位于 `docs/requests/bilicatch-v1-desktop/`。
